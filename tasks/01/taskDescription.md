# Print id

For the mvp (described at docs/mvp.md), we need to easily print the user id so we can pass it on to other people.  
There should be a program argument to print the id.  
The should be a 
--help
to show all arguments.
Please, add a help and a --id to print the current id