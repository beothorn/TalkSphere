# Common module

This module contains tiny shared contracts used across unrelated modules.

## Responsibilities

- Provide the shared `TALKSPHERE_SUCCESS` and `TALKSPHERE_FAILURE` result codes.

## Boundaries

Keep this module intentionally small. If a concept belongs to one domain, it should stay in that domain instead of being added here for convenience.
